<%= form_for(@card) do |f| %>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="form-group">
    <%= f.label :course %>
    <%= f.text_field :course %>
  </div>

  <div class="form-actions">
    <%= f.submit %>
  </div>
<% end %>